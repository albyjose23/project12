<article class="panel">
  <div class="panel-header">
    <div>
      <h3><%= title %></h3>
      <p class="subtle-text"><%= subtitle %></p>
    </div>
    <span class="badge"><%= badge_text %></span>
  </div>

  <div class="quick-list">
    <% if questions.any? %>
      <% questions.each do |q| %>
        <div class="quick-item-manage">
          <div class="item-body">
            <div class="item-meta">
              <span class="strong text-white"><%= q.subject.code %></span>
              <span class="subtle-text">Unit <%= q.unit.presence || "N/A" %></span>
              <span class="badge" data-difficulty="<%= q.difficulty.to_s.downcase %>">
                <%= q.difficulty.presence || "Unknown" %>
              </span>
              <span class="subtle-text"><%= q.display_marks %> Marks</span>
              <span class="subtle-text">Section <%= q.section_name %></span>
              <span class="subtle-text"><%= q.entry_mode_label %></span>
            </div>
            <div class="item-content text-white">
              <%= truncate(q.content, length: 120) %>
            </div>
          </div>

          <div class="item-actions">
            <%= link_to edit_question_path(q), class: "action-icon edit", title: "Edit" do %>
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.828 2.828 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5L17 3z"></path></svg>
            <% end %>

            <%= link_to delete_question_path(q),
                        data: { "turbo-method": :delete, "turbo-confirm": "Are you sure you want to delete this question?" },
                        class: "action-icon delete",
                        title: "Delete" do %>
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"></polyline><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path><line x1="10" y1="11" x2="10" y2="17"></line><line x1="14" y1="11" x2="14" y2="17"></line></svg>
            <% end %>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="p-8 text-center muted">
        <p><%= empty_message %></p>
      </div>
    <% end %>
  </div>
</article>
